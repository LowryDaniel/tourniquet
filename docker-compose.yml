services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tourniquet
      POSTGRES_PASSWORD: tourniquet
      POSTGRES_DB: tourniquet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tourniquet"]
      interval: 5s
      timeout: 3s
      retries: 10

  web:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://tourniquet:tourniquet@db:5432/tourniquet
      APP_BASE_URL: http://localhost:8000
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./templates:/app/templates
      - ./static:/app/static
    command: uvicorn tourniquet.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  pgdata:
